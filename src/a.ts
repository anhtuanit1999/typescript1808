let age: number = 100;
function sayAge(age:number): string {
    return `Hello ${age}`;
}

const sayHello = (name: string): string => {
    return `Hello ${name}`;
};

console.log(sayAge(age), sayHello('tuan'));

